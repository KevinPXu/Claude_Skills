# Eval 3: New Project — New Skill Response

I checked your memory and confirmed your preference for bun over npm (from your Preferences note).

I've saved the project context for future sessions:

**Created** `Projects/Weather-Dashboard.md` with:
- Tech stack: React 18, TypeScript, Vite, bun, OpenWeatherMap API
- Each choice documented with reasoning
- bun preference linked back to [[Preferences]] where it's already recorded
- Linked to [[Projects]] index

For the project setup itself, I'd scaffold with:
```bash
cd ~/weather-dashboard
bun create vite . --template react-ts
bun install
```

Next session in this directory, I'll automatically load the Weather Dashboard context so we can pick up where we left off.
