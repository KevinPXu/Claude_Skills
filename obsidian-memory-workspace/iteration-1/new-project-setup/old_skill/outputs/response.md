# Eval 3: New Project — Old Skill Response

I've saved the project context to memory.

Created `Projects/Weather-Dashboard.md` with:
- React, TypeScript, Vite, bun, OpenWeatherMap API
- Working directory: ~/weather-dashboard

Linked to Projects index.

For project setup:
```bash
cd ~/weather-dashboard
bun create vite . --template react-ts
bun install
```
